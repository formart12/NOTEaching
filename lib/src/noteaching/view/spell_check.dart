import 'package:flutter/material.dart';

class SpellCheck extends StatelessWidget {
  const SpellCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
